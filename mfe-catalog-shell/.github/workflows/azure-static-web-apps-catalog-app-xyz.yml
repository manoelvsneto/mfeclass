name: Deploy Catalog App to Azure Static Web Apps

on:
  push:
    branches:
      - main
    paths:
      - 'catalog/**'
      - '.github/workflows/azure-static-web-apps-catalog-app-xyz.yml'
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main
    paths:
      - 'catalog/**'
      - '.github/workflows/azure-static-web-apps-catalog-app-xyz.yml'

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Catalog App
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true

      - name: Setup Node.js 20.x
        uses: actions/setup-node@v3
        with:
          node-version: 20.x
          cache: 'npm'
          cache-dependency-path: 'catalog/package-lock.json'

      - name: Install dependencies
        run: npm ci
        working-directory: ./catalog

      - name: Build
        run: npm run build
        working-directory: ./catalog

      - name: Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_CATALOG_APP_XYZ }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "/catalog"
          api_location: ""
          output_location: "dist"

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_CATALOG_APP_XYZ }}
          action: "close"
